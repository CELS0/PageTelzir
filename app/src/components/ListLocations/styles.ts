import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        height: 255,
        width: 320,
        backgroundColor: '#2d3644',
        borderRadius: 4,
    },
    title: {
        fontSize: 16,
        color: '#ffff',
    }
})